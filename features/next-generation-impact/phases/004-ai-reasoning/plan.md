# Phase 004 plan

1. Add neutral protocol/schema package and fake provider conformance.
2. Add deterministic-seed retrieval planner with scoped capabilities and hard budgets.
3. Add minimized/redacted evidence request construction.
4. Add strict response parser, citation resolver, and candidate policy.
5. Store reasoning runs/hypotheses separately through additive storage records.
6. Add independent timeout/cancellation/circuit-breaker and deterministic merge isolation.
7. Add consent, deletion, telemetry, injection, malformed-output, and replay tests.
8. Document provider adapter obligations without implementing or enabling a vendor.
