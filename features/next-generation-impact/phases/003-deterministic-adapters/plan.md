# Phase 003 plan

For each family, in order events → database → HTTP → configuration → infrastructure:

1. freeze identity components and collision/equivalence fixtures;
2. add manifest, supported input classifier, parser, definitions/references, and coverage;
3. add compatibility and activation behavior;
4. add semantic partitions, dependency closure, replacements, and tombstones;
5. add cross-repository, exact-head same-repository, dynamic/partial, and adversarial fixtures;
6. add admission, docs, licenses, performance observations, and disabled-family regression;
7. land the independently reviewable vertical slice before beginning the next.
